O comando LIMIT, SELECT TOP ou ROWNUMé usado para especificar o número de registros a serem retornados.

A instrução SQL a seguir seleciona os três primeiros registros da tabela "Clientes" (SQL SERVER):

SELECT TOP 3 * FROM clientes;
