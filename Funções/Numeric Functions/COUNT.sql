Retorne a quantidade de produtos na tabela "Produtos":
SELECT COUNT(produtoID) numeroDeProdutos FROM produtos;

A função COUNT() retorna o número de registros retornados por uma consulta selecionada.

Nota: Valores NULL não são contados.
COUNT(expression)
