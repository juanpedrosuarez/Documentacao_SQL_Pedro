Retorne a posição do índice "q" na lista de strings:

A função FIELD() retorna a posição do índice de um valor em uma lista de valores.
FIELD(value, val1, val2, val3, ...)

Retorne a posição do índice "a" na lista de strings:
SELECT FIELD("a", "p", "r", "a");

Funciona com numeros
