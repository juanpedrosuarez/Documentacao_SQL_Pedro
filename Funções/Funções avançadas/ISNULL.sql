A função ISNULL() retorna 1 ou 0 dependendo se uma expressão é NULL.

Se a expressão for NULL, esta função retornará 1. Caso contrário, retornará 0.
ISNULL(expression)
  
Teste se uma expressão é NULL:
SELECT ISNULL(NULL);
